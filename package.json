{
  "name": "0xcyberlitech-portfolio-tests",
  "type": "module",
  "scripts": {
    "test": "jest --coverage",
    "build": "node build/build.cjs"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "esbuild": "^0.19.0"
  }
}
